{
  "name": "secure-evidence-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security rules tests for the Secure Evidence backend. Run against the Firebase emulator: firebase emulators:exec --only auth,firestore,storage \"npm --prefix test/rules test\"",
  "scripts": {
    "test": "mocha --timeout 20000 evidence_rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0",
    "mocha": "^10.8.2"
  }
}
